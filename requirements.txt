# Python 3.14 — install with: pip install -r requirements.txt --only-binary :all:
# torch / ultralytics NOT used — replaced by onnxruntime + OpenCV for AppLocker compat.
numpy>=2.0
opencv-python>=4.10,<5
insightface==1.0.1
onnxruntime>=1.20,<2
PyYAML>=6.0,<7
fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
